# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか

type,name,value属性のあるinputタグ。

### findメソッドの引数に指定しているIDは何のIDか

todoテーブルのid。

### findメソッドで実行しているSQLは何か

SELECT~WHERE文。

### findメソッドで取得できる値は何か

呼び出されたメソッドの引数と、findメソッドの引数が一致する値。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか

既存データと更新するデータに違いがあるかどうかを基準にしている。

## Todo論理削除

### traitとclassの違いとは

クラスはインスタンス化してメソッドとして呼び出したりすることができるが、トレイトは定義してuseをすることでそのメソッドを呼び出すことができる。

### traitを使用するメリットとは

複数のクラスで同じメソッドやプロパティを共有できるため、クラス間で同じコードを書かなくても良くなる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか

新しいインスタンスが作成される時に実行される。

### RequestクラスからFormRequestクラスに変更した理由

FormRequestはRequestクラスで使える機能が内包されているため。

### $errorsのhasメソッドの引数・返り値は何か

引数は項目名、返り値は真偽値が返される。

### $errorsのfirstメソッドの引数・返り値は何か

引数は項目名、返り値はmessagesメソッドの値が返される。

### フレームワークとは何か

あらかじめディレクトリなどの構成が決まっており、既にある程度の枠組みが存在する状態でアプリケーションの作成を行うことができるもの。

### MVCはどういったアーキテクチャか

データの役割ごとに管理する部門を分けて、リクエストに応じて処理をする構造。

### ORMとは何か、またLaravelが使用しているORMは何か

ORMは、ModelとDB上のテーブルをマッピングすることで、SQL側で操作することなくDB上にアクセスすることができる機能。
LaravelではEloquentを使っている。

### composer.json, composer.lockとは何か

composer.jsonに書かれた内容を元にパッケージをインストールし、それと同時にcomposer.lockファイルを作成している。2回目以降のインストール後は、composer.lockを元にインストールしている。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか

vendorディレクトリ。
